pkgname=sudo-destroyers
pkgver=2.0
pkgrel=1
pkgdesc="A harmless terminal RPG with battles, levels, upgrades, and save files"
arch=('any')
license=('MIT')
depends=('bash')
source=()
sha256sums=()

package() {
    install -Dm755 "$startdir/sudo-destroyers-game" "$pkgdir/usr/bin/sudo-destroyers"
}
