pkgname=ventoy-terminal
pkgver=1.0
pkgrel=1
pkgdesc="Terminal-based Ventoy-style USB multiboot manager for MSYS2"
arch=('any')
license=('MIT')
depends=('bash')
source=()
sha256sums=()

package() {
    install -Dm755 "$startdir/ventoy-terminal" "$pkgdir/usr/bin/ventoy-terminal"
}
